An REST application for message manipulations. Created using Spring Boot with Spring Web.

Build project:
```shell
  mvn clean install
```

Run project:
```shell
  java -jar target/servlet-0.0.1-SNAPSHOT.jar
```