An REST application for message manipulations. Created using Spring Boot with Spring WebFlux.

Build project:
```shell
  mvn clean install
```

Run project:
```shell
  java -jar target/reactive-0.0.1-SNAPSHOT.jar
```